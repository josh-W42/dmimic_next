import Image from 'next/image';
import Link from 'next/link';

export default function Home() {
  return (
    <main>
      {/* <Link href={'/app/servers/[slug]'}>
        <span>To a server</span>
      </Link> */}
    </main>
  );
}
